version: 1
runtime: python
entry: hebbian_vault/server.py

build:
  install: pip install -e .

startCommand:
  type: http
  command: python -m hebbian_vault --transport streamable-http

configSchema:
  source: code
